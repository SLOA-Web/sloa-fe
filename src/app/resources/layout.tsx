export const metadata = {
  title: 'Resources - Sri Lanka Orthopaedic Association',
};

export default function ResourcesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}